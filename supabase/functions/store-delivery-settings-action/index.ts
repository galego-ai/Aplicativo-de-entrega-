import { withSupabase } from "npm:@supabase/server@1.4.1";

type Body={storeId:string;pricingModel:"FREE"|"FIXED"|"DISTANCE";fixedFee:number;baseFee:number;perKmFee:number;minimumFee:number;maxRadiusKm:number|null;pickupEnabled:boolean;clickfoodDeliveryEnabled:boolean;ownDeliveryEnabled:boolean};
const finite=(v:number)=>Number.isFinite(v)&&v>=0;
export default{fetch:withSupabase({auth:"user"},async(req,ctx)=>{if(req.method!=="POST")return Response.json({error:"METHOD_NOT_ALLOWED"},{status:405});let body:Body;try{body=await req.json()}catch{return Response.json({error:"INVALID_JSON"},{status:400})}if(!body.storeId||!["FREE","FIXED","DISTANCE"].includes(body.pricingModel))return Response.json({error:"INVALID_SETTINGS"},{status:400});const role=String(ctx.userClaims!.appMetadata?.clickfood_role??"");let allowed=["SUPER_ADMIN","ADMIN"].includes(role);if(!allowed){const{data}=await ctx.supabaseAdmin.from("store_memberships").select("id").eq("store_id",body.storeId).eq("user_id",ctx.userClaims!.id).eq("active",true).in("role",["OWNER","MANAGER"]).maybeSingle();allowed=!!data;}if(!allowed)return Response.json({error:"STORE_MANAGER_REQUIRED"},{status:403});const values=[body.fixedFee,body.baseFee,body.perKmFee,body.minimumFee];if(!values.every(finite))return Response.json({error:"INVALID_FEES"},{status:400});if(body.maxRadiusKm!=null&&(!Number.isFinite(body.maxRadiusKm)||body.maxRadiusKm<=0||body.maxRadiusKm>200))return Response.json({error:"INVALID_RADIUS"},{status:400});const{data:before}=await ctx.supabaseAdmin.from("store_delivery_settings").select("*").eq("store_id",body.storeId).maybeSingle();const row={store_id:body.storeId,pricing_model:body.pricingModel,fixed_fee:body.fixedFee,base_fee:body.baseFee,per_km_fee:body.perKmFee,minimum_fee:body.minimumFee,max_radius_km:body.maxRadiusKm,pickup_enabled:!!body.pickupEnabled,clickfood_delivery_enabled:!!body.clickfoodDeliveryEnabled,own_delivery_enabled:!!body.ownDeliveryEnabled,updated_at:new Date().toISOString()};const{data,error}=await ctx.supabaseAdmin.from("store_delivery_settings").upsert(row,{onConflict:"store_id"}).select("*").single();if(error)return Response.json({error:"DELIVERY_SETTINGS_SAVE_FAILED"},{status:500});await ctx.supabaseAdmin.from("audit_logs").insert({actor_id:ctx.userClaims!.id,action:"STORE_DELIVERY_SETTINGS_UPDATED",entity_type:"store",entity_id:body.storeId,before_data:before??null,after_data:data});return Response.json({settings:data});})};
