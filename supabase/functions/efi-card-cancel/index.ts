import { withSupabase } from "npm:@supabase/server@1.4.1";
const env=(n:string)=>Deno.env.get(n)?.trim()||"";const base=()=>env("EFI_PIX_SANDBOX")==="false"?"https://cobrancas.api.efipay.com.br":"https://cobrancas-h.api.efipay.com.br";const creds=()=>({id:env("EFI_CHARGES_CLIENT_ID")||env("EFI_PIX_CLIENT_ID"),secret:env("EFI_CHARGES_CLIENT_SECRET")||env("EFI_PIX_CLIENT_SECRET")});async function auth(){const c=creds();const r=await fetch(`${base()}/v1/authorize`,{method:"POST",headers:{Authorization:`Basic ${btoa(`${c.id}:${c.secret}`)}`,"Content-Type":"application/json"},body:'{"grant_type":"client_credentials"}'});const d=await r.json().catch(()=>({}));if(!r.ok||!d.access_token)throw new Error(`EFI_AUTH_${r.status}`);return String(d.access_token)}
export default{fetch:withSupabase({auth:"user"},async(req,ctx)=>{if(req.method!=="POST")return Response.json({error:"METHOD_NOT_ALLOWED"},{status:405});let b:any;try{b=await req.json()}catch{return Response.json({error:"INVALID_JSON"},{status:400})}const orderId=String(b.orderId??"");if(!orderId)return Response.json({error:"ORDER_REQUIRED"},{status:400});const role=String(ctx.userClaims!.appMetadata?.clickfood_role??"");const{data:o}=await ctx.supabaseAdmin.from("orders").select("id,store_id,customer_id").eq("id",orderId).maybeSingle();if(!o)return Response.json({error:"ORDER_NOT_FOUND"},{status:404});let allowed=o.customer_id===ctx.userClaims!.id||["SUPER_ADMIN","ADMIN","SUPPORT"].includes(role);if(!allowed){const{data:m}=await ctx.supabaseAdmin.from("store_memberships").select("role").eq("store_id",o.store_id).eq("user_id",ctx.userClaims!.id).eq("active",true).maybeSingle();allowed=!!m&&["OWNER","MANAGER"].includes(String(m.role))}if(!allowed)return Response.json({error:"CANCEL_DENIED"},{status:403});const{data:p}=await ctx.supabaseAdmin.from("payments").select("id,status,method,provider").eq("order_id",orderId).eq("method","CREDIT_CARD").order("created_at",{ascending:false}).limit(1).maybeSingle();if(!p)return Response.json({ok:true,localOnly:true});if(p.status==="PAID")return Response.json({error:"PAYMENT_ALREADY_PAID"},{status:409});const{data:ch}=await ctx.supabaseAdmin.from("efi_card_charges").select("id,charge_id,status").eq("payment_id",p.id).maybeSingle();if(!ch?.charge_id)return Response.json({ok:true,localOnly:true});if(ch.status==="CANCELED")return Response.json({ok:true,reused:true});if(ch.status==="PAID")return Response.json({error:"PAYMENT_ALREADY_PAID"},{status:409});if(ch.status==="APPROVED")return Response.json({error:"PAYMENT_SETTLEMENT_PENDING"},{status:409});let access:string;try{access=await auth()}catch{return Response.json({error:"EFI_CARD_AUTH_FAILED"},{status:502})}const r=await fetch(`${base()}/v1/charge/${ch.charge_id}/cancel`,{method:"PUT",headers:{Authorization:`Bearer ${access}`,"Content-Type":"application/json"}});const d=await r.json().catch(()=>({}));if(!r.ok){if(r.status===400||r.status===409)return Response.json({error:"CARD_CHARGE_CANNOT_BE_CANCELLED",providerStatus:r.status},{status:409});return Response.json({error:"EFI_CARD_CANCEL_FAILED",providerStatus:r.status},{status:502})}await Promise.all([ctx.supabaseAdmin.from("efi_card_charges").update({status:"CANCELED",provider_payload:d,updated_at:new Date().toISOString()}).eq("id",ch.id),ctx.supabaseAdmin.from("payments").update({status:"CANCELLED"}).eq("id",p.id)]);return Response.json({ok:true,status:"CANCELED"})})};