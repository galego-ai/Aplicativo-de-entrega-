import { withSupabase } from "npm:@supabase/server@1.4.1";

const dayMap:Record<string,number>={Sun:0,Mon:1,Tue:2,Wed:3,Thu:4,Fri:5,Sat:6};
type HoursRow={store_id:string;weekday:number;opens_at:string|null;closes_at:string|null;closed:boolean};
function clockParts(timezone:string){const fmt=new Intl.DateTimeFormat("en-US",{timeZone:timezone,weekday:"short",hour:"2-digit",minute:"2-digit",hourCycle:"h23"});const parts=Object.fromEntries(fmt.formatToParts(new Date()).map(p=>[p.type,p.value]));return{weekday:dayMap[parts.weekday]??0,minutes:Number(parts.hour)*60+Number(parts.minute)};}
function hm(v:string|null){if(!v)return null;const[h,m]=v.slice(0,5).split(":").map(Number);return h*60+m;}
function isOpen(rows:HoursRow[],timezone:string){if(!rows.length)return true;const{weekday,minutes}=clockParts(timezone);const today=rows.find(r=>r.weekday===weekday);if(today&&!today.closed){const open=hm(today.opens_at),close=hm(today.closes_at);if(open==null&&close==null)return true;if(open!=null&&close!=null){if(open===close)return true;if(open<close&&minutes>=open&&minutes<close)return true;if(open>close&&minutes>=open)return true;}}const prev=rows.find(r=>r.weekday===((weekday+6)%7));if(prev&&!prev.closed){const open=hm(prev.opens_at),close=hm(prev.closes_at);if(open!=null&&close!=null&&open>close&&minutes<close)return true;}return false;}
export default{fetch:withSupabase({auth:"user"},async(req,ctx)=>{if(req.method!=="GET"&&req.method!=="POST")return Response.json({error:"METHOD_NOT_ALLOWED"},{status:405});const{data:stores,error}=await ctx.supabaseAdmin.from("stores").select("id,name,description,logo_url,cover_url,minimum_order,average_preparation_time,city_id,cities(timezone)").eq("status","ACTIVE").order("name").limit(100);if(error)return Response.json({error:"STORE_CATALOG_FAILED"},{status:500});const ids=(stores??[]).map((s:any)=>s.id);const{data:hours}=ids.length?await ctx.supabaseAdmin.from("store_business_hours").select("store_id,weekday,opens_at,closes_at,closed").in("store_id",ids):{data:[] as HoursRow[]};const byStore=new Map<string,HoursRow[]>();for(const row of (hours??[]) as HoursRow[]){const list=byStore.get(row.store_id)??[];list.push(row);byStore.set(row.store_id,list);}const result=(stores??[]).map((s:any)=>{const city=Array.isArray(s.cities)?s.cities[0]:s.cities;const timezone=String(city?.timezone??"America/Sao_Paulo");return{id:s.id,name:s.name,description:s.description,logo_url:s.logo_url,cover_url:s.cover_url,minimum_order:Number(s.minimum_order),average_preparation_time:Number(s.average_preparation_time),timezone,open_now:isOpen(byStore.get(s.id)??[],timezone)};});return Response.json({stores:result,generatedAt:new Date().toISOString()});})};
