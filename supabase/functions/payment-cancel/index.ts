import { withSupabase } from "npm:@supabase/server@1.4.1";
export default{fetch:withSupabase({auth:"user"},async(req,ctx)=>{if(req.method!=="POST")return Response.json({error:"METHOD_NOT_ALLOWED"},{status:405});let b:any;try{b=await req.json()}catch{return Response.json({error:"INVALID_JSON"},{status:400})}const orderId=String(b.orderId??"");if(!orderId)return Response.json({error:"ORDER_REQUIRED"},{status:400});const{data:p}=await ctx.supabaseAdmin.from("payments").select("provider,method,status").eq("order_id",orderId).order("created_at",{ascending:false}).limit(1).maybeSingle();if(!p)return Response.json({ok:true,localOnly:true});if(p.status==="PAID")return Response.json({error:"PAYMENT_ALREADY_PAID"},{status:409});if(p.provider!=="EFI")return Response.json({ok:true,localOnly:true});const target=p.method==="PIX"?"efi-pix-cancel":p.method==="CREDIT_CARD"?"efi-card-cancel":"";if(!target)return Response.json({ok:true,localOnly:true});const auth=req.headers.get("Authorization")??"";const r=await fetch(`${Deno.env.get("SUPABASE_URL")}/functions/v1/${target}`,{method:"POST",headers:{Authorization:auth,"Content-Type":"application/json"},body:JSON.stringify({orderId})});let d:any={};try{d=await r.json()}catch{}return Response.json(d,{status:r.status})})};