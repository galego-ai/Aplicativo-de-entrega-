name: CLICK-FOOD Mobile Shell Patch (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow temporário desativado. Nenhuma alteração automática será executada."
